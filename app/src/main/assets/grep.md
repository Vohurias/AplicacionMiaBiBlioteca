# Comando `grep` (Global Regular Expression Print)

Busca líneas que coincidan con un patrón o texto dentro de un archivo.

## Ejemplos prácticos:
* `grep "error" log.txt` : Filtra todas las líneas con la palabra "error".
* `grep -i "wlan" dmesg` : Busca ignorando mayúsculas/minúsculas.
# Comando `ls` (Listar)

Muestra el contenido del directorio de trabajo actual o del especificado.

## Ejemplos en Termux:
* `ls -la` : Muestra permisos detallados e incluye archivos ocultos.
* `ls -lh` : Muestra los tamaños en formato legible (KB, MB).